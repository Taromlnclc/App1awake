<resources>
    <!-- Base application theme. -->
    <style name="Theme.temaAwakedia" parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/fondo</item>
        <item name="colorPrimaryDark">@color/marca</item>
        <item name="colorAccent">@color/select</item>
        <item name="android:textColor">#7700FF</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">#42FF3B</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.temaAwakenoche" parent="Theme.AppCompat">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/fondo</item>
        <item name="colorPrimaryDark">@color/marca</item>
        <item name="colorAccent">@color/select</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">#E91E63</item>
        <item name="android:textSize">20sp</item>
        <item name="android:background">#333A33</item>
    </style>
</resources>